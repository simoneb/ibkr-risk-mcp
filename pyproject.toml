[build-system]
# 77 or newer for PEP 639 licence metadata (the `license` SPDX expression and
# `license-files`), which older setuptools ignores or rejects.
requires = ["setuptools>=77", "wheel"]
build-backend = "setuptools.build_meta"

[project]
name = "ibkr-risk-mcp"
version = "0.3.0"
description = "MCP server exposing Interactive Brokers portfolio risk: model greeks, IB's volatility surface, what-if margin, and a local stress engine"
readme = "README.md"
requires-python = ">=3.11"
license = "MIT"
license-files = ["LICENSE"]
authors = [
  { name = "Simone Busoli" }
]
keywords = ["mcp", "ibkr", "interactive-brokers", "tws", "risk", "options", "greeks", "stress-test"]
classifiers = [
  "Development Status :: 3 - Alpha",
  "Environment :: Console",
  "Intended Audience :: Financial and Insurance Industry",
  "Operating System :: OS Independent",
  "Programming Language :: Python :: 3",
  "Programming Language :: Python :: 3.11",
  "Programming Language :: Python :: 3.12",
  "Programming Language :: Python :: 3.13",
  "Topic :: Office/Business :: Financial :: Investment",
]
dependencies = [
  # 1.10 is the floor for the OAuth resource-server surface: AuthSettings grew
  # `resource_server_url` there, and with it the RFC 9728 metadata route and
  # the WWW-Authenticate challenge that the auth module relies on the SDK to
  # serve. Verified against the 1.9.4 and 1.10.0 wheels.
  "mcp>=1.10.0,<2.0",
  "ib-async>=1.0.3,<3.0",
  "numpy>=1.26",
  "scipy>=1.11",
  "python-dotenv>=1.0,<2.0",
  # Both arrive transitively with mcp today, and both are imported directly by
  # `auth`, which makes them ours to pin rather than ours to inherit. The
  # `crypto` extra is what supplies RS256 and friends; PyJWT without it can
  # only do HMAC, which this server refuses.
  "pyjwt[crypto]>=2.8,<3.0",
  "httpx>=0.27",
]

[project.optional-dependencies]
dev = ["pytest>=8.0,<9.0", "pytest-asyncio>=0.23,<2.0"]

[project.scripts]
ibkr-risk-mcp = "ibkr_risk_mcp.server:main"

[tool.setuptools]
package-dir = {"" = "src"}

[tool.setuptools.packages.find]
where = ["src"]

[tool.pytest.ini_options]
testpaths = ["tests"]
asyncio_mode = "auto"
